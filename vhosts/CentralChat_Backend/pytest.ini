# E2E — requires docker-compose.dev.yml stack (see scripts/run_e2e.sh)

[pytest]
markers =
    e2e: end-to-end tests against live stack (docker-compose.dev.yml)
    e2e_llm: e2e tests that call real LLM (needs OPENROUTER_API_KEY)

testpaths = tests
