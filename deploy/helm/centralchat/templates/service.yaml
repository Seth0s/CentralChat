apiVersion: v1
kind: Service
metadata:
  name: {{ include "centralchat.fullname" . }}
  labels:
    {{- include "centralchat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "centralchat.selectorLabels" . | nindent 4 }}
